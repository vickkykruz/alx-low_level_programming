#include <stdio.h>
/**
 * main - print the name
 * @argc: The number of arguments
 * @argv: The vector one-dimenstion
 * Return: 0
*/
int main(int argc, char *argv[])
{
if (argc == 1)
{
printf("%s\n", argv[0]);
}
return (0);
}
