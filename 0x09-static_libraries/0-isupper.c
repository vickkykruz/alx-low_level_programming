#include "main.h"
/**
 * _isupper - check the uppercase character
 * @c: The input value
 * Return: 1 if is uppercase or 0
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
return (0);
}
