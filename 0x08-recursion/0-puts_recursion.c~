#include "main.h"
/**
 * _puts_recursion - print a string
 * @s: The input string
 * Return: void
*/
void _puts_recursion(char *s)
{
/* Base Case*/
if (*s == '\0')
{
return;
_putchar('\n');
}
_putchar(*s);
_puts_recursion(s + 1);
}
