#include "main.h"
#include <ctype.h>
/**
 * _islower - checker function
 * @c: The input cariable
 * Return: 1if lowecase and 0 if is uppercase
*/
int _islower(int c)
{
char c = 'a';
while (c <= 'z')
{
if (c == 'c')
{
return (1);
}
c++;
}
return (0);
}
