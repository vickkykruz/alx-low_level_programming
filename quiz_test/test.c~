#include <stdio.h>
int main(void)
{
  int i;
  /* for (i = 0; i < 10; i++)
    {
      printf("%d", i * 2);
    }
  */
    i = 0;
  while(i < 10)
    {
      printf("%d", i % 2);
      i++;
      } 
  printf("\n");
  return (0);
}
